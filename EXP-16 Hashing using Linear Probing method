#include <stdio.h>
#define SIZE 10

int hashTable[SIZE];

void insert(int key)
{
    int index = key % SIZE;
    while (hashTable[index] != 0)
    {
        index = (index + 1) % SIZE;
    }
    hashTable[index] = key;
}

void display()
{
    for (int i = 0; i < SIZE; i++)
    {
        printf("%d ", hashTable[i]);
    }
    printf("\n");
}

void main()
{
    int arr[] = {12, 22, 32, 42, 52};
    for (int i = 0; i < 5; i++)
        insert(arr[i]);
    printf("Hash Table: ");
    display();
}
